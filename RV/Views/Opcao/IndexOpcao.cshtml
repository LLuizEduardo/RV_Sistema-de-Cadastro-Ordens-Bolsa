@model List<OpcaoModel>
@{
    ViewData["Title"] = "Opções";
}


@if (TempData["MensagemSucesso"] != null)
{
    <div class="alert alert-success text-center" role="alert">
        <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">x</button>
        @TempData["MensagemSucesso"]
    </div>
}

<br />

<div class="text-center">
    <h1 class="text-light">Lista de ordens</h1>
</div>

<br />

<body class="bg-dark">

    <div class="text-right">
        <a role="button" asp-controller="Home" asp-action="Index" class="btn btn-secondary btn-sm">Voltar</a>
        <a role="button" class="btn btn-primary btn-sm" asp-controller="Opcao" asp-action="Salvar">Cadastrar nova</a>
    </div>
    <br />


    <table class="table table-dark table-striped rounded-sm">
        <thead>
            <tr>
                <th scope="col">Data</th>
                <th scope="col">Opção</th>
                <th scope="col">Tipo</th>
                <th scope="col">Papel</th>
                <th scope="col">Strike</th>
                <th scope="col">Quantidade</th>
                <th scope="col">Vencimento</th>
                <th scope="col">Valor</th>
                <th scope="col">Ordem</th>
                <th scope="col">Corretora</th>
                <th scope="col"></th>
            </tr>
        </thead>


        @if (Model != null && Model.Any())
        {
            foreach (OpcaoModel Opcao in Model)
            {
                <tbody>
                    <tr>
                        <td>@Opcao.Data.ToString().Substring(0, 10)</td>
                        <td>@Opcao.Opcao</td>
                        <td>@Opcao.Tipo</td>
                        <td>@Opcao.Papel</td>
                        <td>@Opcao.Strike</td>
                        <td>@Opcao.Quantidade</td>
                        <td>@Opcao.Vencimento.ToString().Substring(0, 10)</td>
                        <td>@Opcao.Valor</td>
                        <td>@Opcao.Ordem</td>
                        <td>@Opcao.Corretora</td>
                        <td>
                            <a role="button" asp-route-id="@Opcao.Id" class="btn btn-primary btn-sm" asp-controller="Opcao" asp-action="Editar">Editar</a>
                            <a role="button" asp-route-id="@Opcao.Id" class="btn btn-danger btn-sm" asp-controller="Opcao" asp-action="Apagar">Apagar</a>
                        </td>
                    </tr>
                </tbody>
            }
        }

    </table>
</body>
