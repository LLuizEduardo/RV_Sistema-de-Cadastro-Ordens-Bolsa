@model Workspace
@{
    ViewData["Title"] = "Sistema de cadastro de ordens";
}

<body class="bg-dark">

    <div class="text-center">
        <h1 class="text-light display-4"> Sistema de cadastro de ordens</h1>
        <br />
        <h2 class="text-light">Renda Variável</h2>
    </div>
    <br />
    <div class="row">
        <div class="col">
            <a role="button" class="my-3 btn btn-primary" asp-controller="Acao" asp-action="Index">Módulo de Ações</a>
            <br />
            <table class="table table-dark table-striped rounded-sm">
                <thead>
                    <tr>
                        <th scope="col">Papel</th>
                        <th scope="col">Quantidade</th>
                        <th scope="col">Preço Médio</th>
                        <th scope="col">Valor</th>
                    </tr>
                </thead>
                @*@if (Model != null && Model.Acoes.Any())
        {
            foreach (AcaoModel Acao in Model.Acoes)
            {
                <tbody>
                    <tr>
                        <td>@Acao.Papel</td>
                        <td>@Acao.Quantidade</td>
                        <td>@Acao.Valor</td>
                    </tr>
                </tbody>
            }
        }*@
                @if (Model != null && Model.TotalAcoes.Any())
                {
                    foreach (TotalAcoes Acao in Model.TotalAcoes)
                    {
                        <tbody>
                            <tr>
                                <td>@Acao.Papel</td>
                                <td>@Acao.Quantidade</td>
                                <td>@Acao.PrecoMedio</td>
                                <td>@Acao.Valor</td>
                            </tr>
                        </tbody>
                    }
                }
            </table>
        </div>
        @*<div class="col">
            <a role="button" class="btn btn-primary" asp-controller="Opcao" asp-action="Index">Módulo de Opções</a>
            <br />
            <table class="table table-dark table-striped rounded-sm">
                <thead>
                    <tr>
                        <th scope="col">Papel</th>
                        <th scope="col">Quantidade</th>
                        <th scope="col">Valor</th>
                    </tr>
                </thead>
                @if (Model != null && Model.Opcoes.Any())
                {
                    foreach (OpcaoModel Opcao in Model.Opcoes)
                    {
                        <tbody>
                            <tr>
                                <td>@Opcao.Papel</td>
                                <td>@Opcao.Quantidade</td>
                                <td>@Opcao.Valor</td>
                            </tr>
                        </tbody>
                    }
                }
            </table>
        </div>*@
    </div>
    <br />
</body>