@model UsuarioModel
@{
    ViewData["Title"] = "Apagar ordens";
}

@if (TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger text-center" role="alert">
        <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">x</button>
        @TempData["MensagemErro"]
    </div>
}

<input type="hidden" asp-for="Id" />
<body class="bg-dark">
    <input type="hidden" asp-for="Id" />
    <br />
    <div class="text-center">
        <h1 class="text-light display-4"> Exclusão de Usuarios</h1>
    </div>
    <br />
    <form asp-controller="Usuario" asp-action="Index" class="text-center">
        <input type="hidden" asp-for="Id" />
        <div>
            <h3 class="text-light"> Deseja mesmo excluir este usuario?</h3>
            <br />
        </div>
        <br />
        <div>
            <button type="submit" asp-controller="Usuario" asp-action="Index" class="btn btn-secondary">Cancelar</button>
@*            <button type="submit" asp-route-id="@Model.Id" asp-controller="Usuario" asp-action="Apagar" class="btn btn-danger"> Confirmar</button>*@
            <button type="submit"  asp-controller="Usuario" asp-action="Apagar" class="btn btn-danger"> Confirmar</button>

        </div>
    </form>
</body>